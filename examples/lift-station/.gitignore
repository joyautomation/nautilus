# Local retained state (setpoints, HOA modes) — recreated on first run.
retain.json

# `naut build` output.
lift-station

# hmi/ — the SvelteKit operator screen: never commit its dependencies or
# build output, only the source.
hmi/node_modules
hmi/.svelte-kit
hmi/build
