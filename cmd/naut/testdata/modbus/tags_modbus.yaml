# Generated by `naut modbus import` from the device map. Do not edit —
# re-run the import, or `naut modbus tags modbus_manifest.yaml --map
# devices.yaml` to re-derive this file from the committed manifest (without
# --map the unit/desc/init columns, which only the map knows, are dropped).
#
# Compose it into a project with:  tag-files: [tags/modbus.yaml]
- { name: FAN_Control, role: output, init: 0, desc: "main fan — control word" }
- { name: FAN_Running, role: input, desc: "main fan — running bit of the status word" }
- { name: FAN_Spv, role: input, unit: "Hz", desc: "main fan — actual speed" }
- { name: FAN_Ssp, role: output, init: 0.0, unit: "Hz", desc: "main fan — speed setpoint" }
- { name: FAN_Status, role: input, desc: "main fan — status word" }
- { name: GAS_CH1, role: input, unit: "ppm", desc: "stack analyser — channel 1" }
- { name: GAS_CH2, role: input, unit: "ppm", desc: "stack analyser — channel 2" }
- { name: GAS_CH3, role: input, unit: "ppm", desc: "stack analyser — channel 3" }
- { name: GAS_CH4, role: input, unit: "ppm", desc: "stack analyser — channel 4" }
- { name: GAS_Flow, role: input, unit: "L/min", desc: "stack analyser — sample flow" }
- { name: TC_A_AlmH, role: input, desc: "zone A loop — high temperature alarm" }
- { name: TC_A_MV, role: input, unit: "%", desc: "zone A loop — PID output" }
- { name: TC_A_Tpv, role: input, unit: "degC", desc: "zone A loop — process temperature" }
- { name: TC_A_Tsp, role: output, init: 0.0, unit: "degC", desc: "zone A loop — temperature setpoint" }
- { name: TC_B_AlmH, role: input, desc: "zone B loop — high temperature alarm" }
- { name: TC_B_MV, role: input, unit: "%", desc: "zone B loop — PID output" }
- { name: TC_B_Tpv, role: input, unit: "degC", desc: "zone B loop — process temperature" }
- { name: TC_B_Tsp, role: output, init: 0.0, unit: "degC", desc: "zone B loop — temperature setpoint" }
